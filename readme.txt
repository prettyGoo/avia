1. В main.go указать переменныые DB_USER, DB_PASSWORD, DB_NAME
2. В main.go указать имя файла в корне проекта, который будет парситься (переменная FILE_NAME)
3. В случае, если база уже инициализирована, нужно передать вторым аргументом в функцию InitDB значение false (= не инициализировать бд заново)

Зависимости:
1. "github.com/lib/pq